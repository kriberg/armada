(function () {
    'use strict';

    function TourController($scope) {
    }

    angular.
        module('tourControllers', []).
        controller('TourController', ['$scope', TourController]);
}());